@using Helpers.Extensions;
@using ViewModels.Common.Unit;
@model UnitDetailViewModel
@{
    Layout = null;
}

<div class="row">
    <div class="col-lg-12">
        <div class="row">
            <div class="col-lg-6">
                <div class="mb-3">
                    <label asp-for="Name" class="form-label fw-600"></label>
                    <div>@(string.IsNullOrEmpty(Model.Name) ? "-" : Model.Name)</div>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="mb-3">
                    <label asp-for="CostTrackerUnit" class="form-label fw-600"></label>
                    <div>@(string.IsNullOrEmpty(Model.CostTrackerUnit) ? "-" : Model.CostTrackerUnit)</div>
                </div>
            </div>
        </div>
        <div class="row">
           
           @* <div class="col-lg-6">
                <div class="mb-3">
                    <label asp-for="Type" class="form-label fw-600"></label>
                    <div>@Model.Type.GetDisplayName()</div>
                </div>
            </div>*@
        </div>
    </div>
</div>

