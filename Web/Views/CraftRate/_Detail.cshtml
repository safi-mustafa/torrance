@using ViewModels.OverrideLogs;
@model CraftRateDetailViewModel
@{
    Layout = null;
}

<div class="row">
    <div class="col-lg-12">
        <div class="row">
            <div class="col-lg-12">
                <div class="mb-3">
                    <label asp-for="Rate" class="form-label fw-600"></label>
                    <div>@(Model.Rate == 0 ? "-" : Model.Rate)</div>
                </div>
            </div>
        </div>
    </div>
</div>

