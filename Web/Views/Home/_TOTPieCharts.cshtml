@model ViewModels.Dashboard.DashboardViewModel
@{
    var columnClass = ViewData["ColumnClass"]?.ToString() ?? "col-12";
}

@if (User.IsInRole("SuperAdmin") || User.IsInRole("Administrator") || User.IsInRole("Approver"))
{
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    Total Count by Delay Type (TOT)
                </h5>
                <p class="card-text">
                <div class="am-chart" id="tot-delay-type"></div>
                </p>
            </div>
        </div>
    </div>
    <div class="@columnClass">
        <div class="card w-100">
            <div class="card-body">
                <h5 class="card-title">Total Count by Unit</h5>
                <p class="card-text">
                <div class="am-chart" id="tot-unit-count"></div>
                </p>
            </div>
        </div>
    </div>
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    Total Hours by Unit (TOT)
                </h5>
                <p class="card-text">
                <div class="am-chart" id="tot-unit"></div>
                </p>
            </div>
        </div>
    </div>
}
