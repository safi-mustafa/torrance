@using ViewModels.OverrideLogs;
@model LeadPlannerDetailViewModel
@{
    Layout = null;
}

<div class="row">
    <div class="col-lg-12">
        <div class="row">
            <div class="col-lg-12">
                <div class="mb-3">
                    <label asp-for="Email" class="form-label fw-600"></label>
                    <div>@(string.IsNullOrEmpty(Model.Email) ? "-" : Model.Email)</div>
                </div>
            </div>
        </div>
    </div>
</div>

